fastapi
uvicorn[standard]
python-multipart
pypdf
networkx
scikit-learn
numpy

# OCR pipeline (scanned PDFs, equipment manuals, handwritten logs / photos).
# Falls back gracefully (skips OCR, keeps whatever text was extracted) if the
# system binaries (tesseract-ocr, poppler-utils) or these packages are missing.
pytesseract
pdf2image
Pillow

# NOTE: sentence-transformers and faiss-cpu are intentionally excluded from
# this requirements file — they pull in PyTorch + CUDA (2GB+) which exceeds
# free-tier build limits. The app uses TF-IDF (scikit-learn) instead, which
# is set via VECTOR_BACKEND=tfidf in the Render environment variables.
# To use semantic embeddings locally, install manually:
#   pip install sentence-transformers faiss-cpu
