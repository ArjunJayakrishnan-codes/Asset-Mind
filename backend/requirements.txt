fastapi
uvicorn[standard]
python-multipart
pypdf
networkx
scikit-learn
numpy

# Optional but recommended (better embeddings). Falls back to TF-IDF automatically if not installed.
sentence-transformers
faiss-cpu

# Optional: for real LLM-generated answers instead of extractive/templated answers.
# Set GROQ_API_KEY env var to enable.
anthropic

# OCR pipeline (scanned PDFs, equipment manuals, handwritten logs / photos).
# Falls back gracefully (skips OCR, keeps whatever text was extracted) if the
# system binaries (tesseract-ocr, poppler-utils) or these packages are missing.
pytesseract
pdf2image
Pillow
